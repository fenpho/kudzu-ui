import { defineComponent } from 'vue'
import './index.less'

export default defineComponent({
  name: 'KuTag',

  // 启用了类型推导
  props: {

  },

  methods: {
   
  },

  setup(props, { emit, slots }) {
    return () => (
      <div />
    )
  }
})
